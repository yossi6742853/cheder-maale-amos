# אבטחה ומוכנות לפריסה — cheder-v3

## עמדת אבטחה (מתוקנת מהגרסה הישנה)
| נושא | הגרסה הישנה | cheder-v3 |
|---|---|---|
| מיקום נתונים | GitHub **ציבורי** / Sheets | Supabase **פרטי** |
| הגנה על הנתונים | client-side בלבד (עקיף) | **RLS בצד-שרת** (`supabase/policies.sql`) |
| אימות | admin/6742, סיסמת 1234, OTP צד-לקוח | Supabase Auth (OTP/Magic-Link, צד-שרת) |
| סודות בקוד | טוקנים/מפתחות גלויים | **אין** — רק anon key (בטוח, RLS מגן) |
| הרצת פקודות מחשב | חשוף (pcExecute) | **לא קיים** |

## צ'קליסט אבטחה (עבר ✅)
- [x] אין סוד/טוקן/מפתח ב-`js/**` — רק `SUPABASE_ANON_KEY` (מוגן ב-RLS).
- [x] RLS מופעל על כל הטבלאות, deny-by-default; מנהל=הכל, מורה=כיתה שלו, רפואי=מוגבל.
- [x] הרשאות בצד-לקוח (`filterByPermissions`) הן UX בלבד — האכיפה ב-RLS.
- [x] אין דלתות אחוריות (אין admin/6742, אין 1234).
- [x] אין endpoint להרצת פקודות/מייל.
- [x] `escHtml` בכל הזרקת נתונים ל-DOM.

## צ'קליסט מוכנות לפריסה
- [x] QA ויזואלי מלא: **30/30 מסכים נקיים** (15 מודולים × דסקטופ+מובייל) — קונסולה נקייה, אפס גלישה.
- [x] PWA + offline (`sw.js`).
- [x] RTL עברי מלא, מצב לילה, רספונסיבי.
- [ ] **ליוסף:** יצירת פרויקט Supabase + הרצת `schema.sql`+`policies.sql` + מילוי `config.js` (ראה `SETUP_INSTITUTION.md`).
- [ ] **ליוסף:** הפעלת GitHub Pages + הגדרת Email Auth ב-Supabase.
- [ ] **ליוסף:** הגדרת המשתמש הראשון כ-`מנהל` בטבלת `profiles`.

## הערה
כל עוד `config.js` ריק — האתר במצב **הדגמה** (מסכים בלי נתונים אמיתיים). מרגע שמחברים Supabase — הכל חי ומאובטח.
